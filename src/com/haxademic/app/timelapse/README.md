# TimeLapse
TimeLapse is a simple app that stiches together an image sequence and renders a .mov file. The image directory, filetype and frames per image are configured via the timelapse.properties file.